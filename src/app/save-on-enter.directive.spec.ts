import { SaveOnEnterDirective } from './save-on-enter.directive';

describe('SaveOnEnterDirective', () => {
  it('should create an instance', () => {
    const directive = new SaveOnEnterDirective();
    expect(directive).toBeTruthy();
  });
});
